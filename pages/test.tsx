import { useState } from "react";
import { checkWord } from "../util/supabase";
import { useAuth } from "../util/auth";
const Test = () => {
  return <div className="bg-green-600 w-64">sup</div>;
};

export default Test;
